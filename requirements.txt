# Runtime dependencies for the ParcelPilot backend.
# Introduced incrementally as milestones need them; declared together here
# as the project's dependency manifest (see docs/git-development-plan.md).

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0
PyYAML>=6.0,<7.0

# Structured data ingestion
openpyxl>=3.1,<4.0

# Document ingestion
pypdf>=5.0,<6.0

# Document retrieval (metadata-filtered lexical search — see system-design.md Part P)
rank-bm25>=0.2,<1.0

# Agent orchestration
langgraph>=0.2,<1.0
langchain-core>=0.3,<1.0
anthropic>=0.39,<1.0

# Structured logging (observability — see system-design.md Part O)
structlog>=24.4,<25.0
